node_modules/
dist/
*.tsbuildinfo
.DS_Store
# packed tarballs from `pnpm pack` / publish:dry-run
forestrie-mcp-verify-*.tgz
# differential-test binary cache
node_modules/.cache/
# Release-time self-registration bundle (plan-2609-02 step 2.3): generated by
# scripts/self-register.mjs at release time, never committed. `files` in
# package.json lists it explicitly so it still ships inside the tarball when
# it exists — see docs/self-registration.md.
/fixtures/self/
