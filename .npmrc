engine-strict=true
# The scope resolves from public npmjs only. No token is configured anywhere
# in this repo or its CI: a passing `pnpm install` IS the tokenless-install
# acceptance check (forestrie-cli's ci.yml says the same).
@forestrie:registry=https://registry.npmjs.org/
